class Solution {
public:
    int majorityElement(vector<int>& v) {
        int cnt = 0;
        int el;

        // Find candidate
        for (int i = 0; i < v.size(); i++) {
            if (cnt == 0) {
                cnt = 1;
                el = v[i];
            }
            else if (v[i] == el) {
                cnt++;
            }
            else {
                cnt--;
            }
        }

        // Count candidate
        int cnt1 = 0;

        for (int i = 0; i < v.size(); i++) {
            if (v[i] == el) {
                cnt1++;
            }
        }

        // Check majority
        if (cnt1 > v.size() / 2) {
            return el;
        }

        return -1;
    }
};
